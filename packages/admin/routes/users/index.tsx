import { type PageProps } from "fresh";
import UserManagementIsland from "../../islands/UserManagementIsland.tsx";

export default function UsersPage(props: PageProps) {
  return <UserManagementIsland />;
}