import { PageProps } from "fresh";
import EntityManagementIsland from "../../islands/EntityManagementIsland.tsx";

export default function EntityManagementPage(props: PageProps) {
  return <EntityManagementIsland />;
}
