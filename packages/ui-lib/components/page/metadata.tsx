// Page components don't typically have metadata files like UI components
// These are full page implementations rather than reusable UI components
// If metadata is needed for pages, it would be structured differently

export const pageComponentsMetadata: any[] = [
  // Page components metadata (old ProfileModal has been removed)
];
