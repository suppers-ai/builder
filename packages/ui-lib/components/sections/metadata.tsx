// Section components don't typically have metadata files like UI components
// These are layout/content sections rather than reusable UI components
// If metadata is needed for sections, it would be structured differently

export const sectionComponentsMetadata = [
  // Section components would have different metadata structure
  // focused on layout and content concerns rather than component props
];
