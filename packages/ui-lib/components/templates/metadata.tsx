// Template components don't typically have metadata files like UI components
// These are page templates rather than reusable UI components
// If metadata is needed for templates, it would be structured differently

export const templateComponentsMetadata = [
  // Template components would have different metadata structure
  // focused on template-level concerns rather than component props
];
