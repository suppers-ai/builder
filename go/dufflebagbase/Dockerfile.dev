# Development Dockerfile - uses local binary
FROM alpine:latest

RUN apk --no-cache add ca-certificates postgresql-client

WORKDIR /app

# The binary and other files will be mounted as volumes
# We don't copy them in the image

EXPOSE 8080

# Default command - can be overridden by docker-compose
CMD ["./dufflebag"]